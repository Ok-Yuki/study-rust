fn main() {
    let days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];

    let bytes = [0; 5];
    // output array
    println!("days = {:?}", days);
    println!("bytes = {:?}", bytes);

    let first = days[0];
    let second = days[1];
    println!("first = {}", first);
    println!("second = {}", second);
}
